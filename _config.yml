collections:
    cards:

  
defaults:
    - scope:
        path: ""
        type: "cards"
      values:
        layout: "card"
    - scope:
        path: ""
      values:
        layout: "default"

plugins:
    - jekyll-feed
    - jekyll-sitemap
    - jekyll-seo-tag
